export const ENUM_SCOPE = {
  DRUGSTORE: 'DRUGSTORE',
  PHARMACY: 'PHARMACY',
  CLINIC: 'CLINIC',
};

export const ENUM_ORDER_STATUS = {
  PENDING: 'pending',
  ALL: 'all',
  COMPLETED: 'completed',
  CANCEL: 'cancel',
};

export const ENUM_TYPE = {};

export default {
  ENUM_SCOPE,
  ENUM_TYPE,
};
